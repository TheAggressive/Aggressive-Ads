{
	"name": "aggressive-ads/wordpress-test-runner",
	"description": "The PHPUnit 9.6 runner for the suites that load real WordPress. Quarantined here so the plugin's own composer.json is not held back by it.",
	"type": "project",
	"license": "GPL-2.0-or-later",
	"require-dev": {
		"phpunit/phpunit": "^9.6",
		"wp-phpunit/wp-phpunit": "7.1.0",
		"yoast/phpunit-polyfills": "^4.0"
	},
	"autoload-dev": {
		"psr-4": {
			"Aggressive\\Ads\\Tests\\": "../php/"
		}
	},
	"config": {
		"optimize-autoloader": true,
		"platform": {
			"php": "8.4.1"
		},
		"sort-packages": true
	},
	"minimum-stability": "stable",
	"prefer-stable": true
}
