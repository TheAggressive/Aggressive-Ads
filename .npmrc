# Lifecycle scripts for optional native bindings are not required for this
# plugin's webpack build. Ignoring them avoids pnpm 10+ approve-builds friction
# in CI (`ERR_PNPM_IGNORED_BUILDS`) without weakening the install itself.
ignore-scripts=true
confirmModulesPurge=false
